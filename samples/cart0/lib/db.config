<?php
$db = @new mysqli("localhost", "steve_student", "60307dB", "steve_sakila");

if ($db->connect_errno) {
   printf("Connect Error %d: %s", $db->connect_errno, $db->connect_error);
   exit(0);
}

?>

